To compile, type into the commandline:
mvn -o clean package appbundle:bundle

To run start the server locally:
java -cp target/WorldJam-x.x.x-SNAPSHOT.jar worldjam.exe.MultiSessionServer
where x.x.x-SNAPSHOT is replaced with the latest release or snapshot.


To run client as an application (this brings up the client setup window):

MacOS:
click on the icon for target/WorldJam-x.x.x-SNAPSHOT/WorldJam.app

Any operating system (Linux, MacOS, or Windows), using the command-line:
java -jar target/WorldJam-x.x.x-SNAPSHOT.jar

